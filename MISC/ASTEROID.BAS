RANDOMIZE TIMER
PRINT "ASTEROID BELT"
s = 0
FOR g = 1 TO 10
CLS
a = INT(RND * 18 + 1)
d = INT(RND * 12 + 1)
n = INT(RND * 9 + 1)
FOR i = 1 TO d
PRINT
NEXT i
FOR i = 1 TO n
IF i <> 1 AND i <> 4 AND i <> 7 THEN GOTO 10
PRINT
PRINT TAB(a);

10 PRINT "*";
NEXT i
PRINT
FOR i = 1 TO 2000
q = VAL("0" + INKEY$)
IF q <> 0 THEN GOTO 20
NEXT i
PRINT "CRASHED INTO ASTEROID"
GOTO 40

20 IF q <> n THEN GOTO 30
PRINT "YOU DESTROYED IT"
s = s + 1

30 IF q < n THEN PRINT "NOT STRONG ENOUGH"
IF q > n THEN PRINT "TOO STRONG"

40 FOR i = 1 TO 5000
NEXT i
NEXT g
PRINT "YOU HIT "; s; " out of 10"
END



